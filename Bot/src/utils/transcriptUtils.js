const axios = require("axios");

async function uploadTranscriptToServer(channelName, transcriptContent, serverId, creator) {
    // Sensible Informations
}

module.exports = { uploadTranscriptToServer };
